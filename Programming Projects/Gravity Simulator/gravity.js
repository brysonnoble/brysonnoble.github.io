// https://spark.iop.org/calculating-gravitational-forces
let G = 6.67 * (10 ** -11);

function Simulate () {
  for (let i = 0; i < circleIdCounter; i++) {
    
  }
}

function Distance (x1, y1, x2, y2) {
  return (((x2 - x1) ** 2) + ((y2 - y1) ** 2)) ** 0.5;
}
